# Convert html file to pug

```
  Usage: html2pug [options] < [file]

  Options:

    -f, --fragment          Don't wrap output in <html>/<body> tags
    -t, --tabs              Use tabs instead of spaces
    -h, --help              Show this page
    -v, --version           Show version

  Examples:

    # Accept input from file and write to stdout
    $ html2pug < example.html

    # Or write to a file
    $ html2pug < example.html > example.pug


D:\nodejs\team-manager\views (master)
λ html2pug < upload.html > upload.pug
```